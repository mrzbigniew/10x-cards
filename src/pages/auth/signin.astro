---
import Layout from "@/layouts/Layout.astro";
import SignInForm from "@/components/auth/SignInForm";

const error = Astro.url.searchParams.get("error");
const notice = Astro.url.searchParams.get("notice");
---

<Layout title="Sign in">
  <div class="bg-cosmic flex min-h-screen items-center justify-center p-4">
    <div class="border-border bg-card text-foreground w-full max-w-sm rounded-2xl border p-8 backdrop-blur-xl">
      <h1
        class="mb-6 bg-gradient-to-r from-blue-500 to-purple-500 bg-clip-text text-center text-2xl font-bold text-transparent"
      >
        Zaloguj się
      </h1>
      {
        notice === "account-deleted" && (
          <div class="mb-4 rounded-lg border border-green-500/30 bg-green-500/10 px-4 py-3 text-sm text-green-600 dark:text-green-400">
            Twoje konto zostało usunięte.
          </div>
        )
      }
      <SignInForm serverError={error} client:load />
      <p class="text-muted-foreground mt-4 text-center text-sm">
        Nie masz konta? <a href="/auth/signup" class="text-purple-600 hover:underline dark:text-purple-400"
          >Zarejestruj się</a
        >
      </p>
    </div>
  </div>
</Layout>
